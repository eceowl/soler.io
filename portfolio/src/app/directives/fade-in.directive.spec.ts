import { FadeInOneDirective } from './fade-in-one.directive';

describe('FadeInOneDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInOneDirective();
    expect(directive).toBeTruthy();
  });
});
